# Example environment variables for local development
# Copy this file to .env and fill values

# Postgres connection (used by server and drizzle / Supabase)
# Format examples:
# postgres://USER:PASSWORD@HOST:PORT/DATABASE
# or the Supabase connection string (from project settings)
DATABASE_URL=postgres://postgres:password@db.example.supabase.co:5432/postgres

# OAuth server (for development you can use http://localhost:3000)
OAUTH_SERVER_URL=http://localhost:3000

# Other optional Vite envs (examples)
VITE_APP_TITLE=DecatAPP
VITE_APP_LOGO=
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=
